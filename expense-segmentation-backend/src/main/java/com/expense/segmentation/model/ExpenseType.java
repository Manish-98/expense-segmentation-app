package com.expense.segmentation.model;

/**
 * Enum representing the type of expense or invoice submission. This can be extended in the future
 * to support additional categories.
 */
public enum ExpenseType {
    EXPENSE,
    INVOICE
}
